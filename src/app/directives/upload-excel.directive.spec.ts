import { UploadExcelDirective } from './upload-excel.directive';

describe('UploadExcelDirective', () => {
  it('should create an instance', () => {
    const directive = new UploadExcelDirective();
    expect(directive).toBeTruthy();
  });
});
